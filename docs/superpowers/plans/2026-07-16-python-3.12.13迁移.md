# Python 3.12.13 版本迁移实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将档案本机扫描服务的项目约束、项目 Conda 环境和运行时验证统一到精确的 Python 3.12.13 64 位。

**Architecture:** 保留现有项目级 Conda 前缀 `D:\archive-scan-service\.venv` 和 `python.exe` 入口，不改用新的虚拟环境布局。先在独立临时前缀验证 Python、pytwain 和 TWAINDSM 运行条件，再重建正式前缀，避免在验证失败时破坏现有环境。

**Tech Stack:** Windows 64 位、Python 3.12.13、Conda、pytwain 2.3.0、pytest、Ruff。

---

### Task 1：增加精确 Python 版本运行时测试

**Files:**
- Modify: `tests/unit/test_runtime_environment.py`

- [x] **Step 1：增加精确版本断言**

新增测试，要求 `sys.version_info[:3] == (3, 12, 13)`，并保留 Windows、64 位和 pytwain 导入测试。

- [x] **Step 2：在现有 3.11.15 环境中运行测试确认失败**

Run: `& '.\\.venv\\python.exe' -m pytest tests/unit/test_runtime_environment.py -v`

Expected: 新增的精确 Python 版本测试因当前环境为 3.11.15 而失败。

### Task 2：更新项目版本约束和中文文档

**Files:**
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `docs/superpowers/plans/2026-07-15-python扫描服务实现计划.md`

- [x] **Step 1：锁定项目 Python 版本**

将 `pyproject.toml` 的约束改为：

```toml
requires-python = "==3.12.13"
```

- [x] **Step 2：同步文档和命令**

将旧的 3.11.15 或 3.11 说明更新为 3.12.13；Python Launcher 命令使用 `py -3.12-64`，精确补丁版本用项目解释器单独验证。

### Task 3：验证临时环境并重建正式环境

**Files:**
- No source files

- [x] **Step 1：创建临时环境并安装项目**

```powershell
conda create --prefix D:\archive-scan-service\.venv312 python=3.12.13 pip -y
& 'D:\archive-scan-service\.venv312\python.exe' -m pip install -e '.[dev]'
```

- [x] **Step 2：验证临时环境**

确认精确版本、64 位、pytwain 导入、TWAINDSM.DLL 搜索和运行时单元测试。

- [x] **Step 3：验证成功后重建正式 `.venv`**

```powershell
conda deactivate
conda remove --prefix D:\archive-scan-service\.venv --all -y
conda create --prefix D:\archive-scan-service\.venv python=3.12.13 pip -y
& 'D:\archive-scan-service\.venv\python.exe' -m pip install -e '.[dev]'
```

### Task 4：最终验证和状态复核

**Files:**
- No source files

- [x] **Step 1：使用正式环境运行全部必要检查**

```powershell
& 'D:\archive-scan-service\.venv\python.exe' --version
& 'D:\archive-scan-service\.venv\python.exe' -m pytest -q
& 'D:\archive-scan-service\.venv\python.exe' -m ruff check .
& 'D:\archive-scan-service\.venv\python.exe' -m compileall -q app scripts tests
git diff --check
```

- [x] **Step 2：复核项目解释器、版本约束、分支和工作区状态**

最终必须同时满足：项目解释器输出 `Python 3.12.13`、进程为 64 位、测试和静态检查通过、当前位于 `codex/` 分支，且没有未说明的改动。
